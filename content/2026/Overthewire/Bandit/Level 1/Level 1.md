This level seems to be teaching us how to interact with dashed filename (`-`). Because `-` is used to identify the flag, files with dash in its name confusing the standard commands like `cat, rm, mv, etc`. 

I tried the `--` method, but doesn't seem to work:
![[Pasted image 20260628111515.png]]

Another method is `./` prefix:
![[Pasted image 20260628111541.png]]

works and password for level 2 is: `PK8fYLZg2hnHSz83plBL1iEPKdD3QToB`