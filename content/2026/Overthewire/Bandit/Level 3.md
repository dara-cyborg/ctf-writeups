This level seems to be teaching us how to find the hidden file specified by `.` at the start of the file name:
![[Pasted image 20260628112127.png]]

I use the `-l` flag to display the owner, group, size of files and directory and `-a` to display all the files including the one that starts with `.`

when we move into `inhere` directory and use `ls` there is nothing listed. so run `ls -la`:
![[Pasted image 20260628112416.png]]

So level 4 password: xzTXq1rDJQVVAzdv5cHq1TQytTWufAMq